# 网球社交平台 - 移动端应用

## 📱 项目简介

基于 Expo (Managed Workflow) 构建的 React Native 移动应用，使用 TypeScript 开发。

## 🛠 技术栈

- **框架:** Expo SDK 54
- **语言:** TypeScript
- **UI 库:** React Native
- **状态管理:** Zustand
- **导航:** React Navigation
- **API 请求:** Axios

## 📁 项目结构

```
mobile/
├── src/
│   ├── components/        # 通用组件
│   │   └── common/       # Button, Input, PasswordInput
│   ├── screens/          # 页面组件
│   │   └── auth/        # 认证相关页面
│   ├── navigation/       # 导航配置
│   ├── services/         # API 服务
│   ├── store/           # 状态管理
│   ├── design-tokens/   # 设计规范
│   └── config/          # 配置文件
├── assets/              # 静态资源
├── App.tsx             # 应用入口
├── app.json            # Expo 配置
└── package.json        # 依赖配置
```

## 🚀 快速开始

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm start
```

然后在 Expo Go 中扫描二维码即可。

### 运行平台

```bash
npm run android    # Android
npm run ios        # iOS
npm run web        # Web
```

## ✨ 已实现功能

### 认证模块
- ✅ 用户登录
- ✅ 用户注册
- ✅ 忘记密码流程
- ✅ Token 管理和自动刷新
- ✅ 认证状态持久化

### 基础组件
- ✅ Button（多种变体）
- ✅ Input（带错误提示）
- ✅ PasswordInput（密码强度检测）

### 设计系统
- ✅ 颜色规范（基于 Figma）
- ✅ 字体规范
- ✅ 间距规范

## ⚙️ 配置

### API 配置

修改 `src/config/api.ts` 中的 `BASE_URL` 来配置后端 API 地址。

### 依赖管理

项目使用 `.npmrc` 配置了 `legacy-peer-deps=true` 来解决 React 19 的依赖冲突。

## 📚 设计对照

所有页面都按照 Figma 设计实现：
- **登录**: 03-01 到 03-04
- **注册**: 05-01 到 05-05
- **忘记密码**: 04-01 到 04-06

详细设计索引请参考：`项目介绍/Tennis-Frog设计索引.md`

## 🔧 开发注意事项

### React Navigation 依赖

确保以下依赖已安装：
- `react-native-gesture-handler`（必须在 App.tsx 最顶部导入）
- `react-native-screens`
- `react-native-safe-area-context`

### 资源文件

- `assets/images/` 目录用于存放图片资源
- 应用图标可以在后续添加（目前使用默认配置）

## 📝 下一步

1. 连接后端 API
2. 实现首页动态流
3. 实现约球功能
4. 实现个人中心

## 🆘 常见问题

### 依赖冲突
如果遇到依赖冲突，运行：
```bash
npm install --legacy-peer-deps
```

### Expo Go 版本不匹配
确保 Expo Go 版本与项目 SDK 版本匹配（当前为 SDK 54）。

---

**开发愉快！** 🎾
