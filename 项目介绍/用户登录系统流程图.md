# 用户登录系统流程图

## 一、用户注册流程

```mermaid
flowchart TD
    Start([用户打开注册页面]) --> Input[输入注册信息]
    Input --> InputDetails[填写用户名、邮箱、密码]
    
    InputDetails --> CheckUsername{实时检查<br/>用户名可用?}
    CheckUsername -->|不可用| ShowError1[显示用户名已存在]
    ShowError1 --> Input
    CheckUsername -->|可用| CheckPassword{检查密码强度}
    
    CheckPassword -->|弱密码| ShowWeak[显示密码强度: 弱]
    ShowWeak --> Input
    CheckPassword -->|中等| ShowMedium[显示密码强度: 中]
    ShowMedium --> ClickRegister
    CheckPassword -->|强密码| ShowStrong[显示密码强度: 强]
    ShowStrong --> ClickRegister
    
    ClickRegister[点击注册按钮] --> ValidateForm{前端验证}
    
    ValidateForm -->|验证失败| ShowError2[显示错误提示]
    ShowError2 --> Input
    
    ValidateForm -->|验证通过| SendRequest[发送注册请求到后端]
    
    SendRequest --> ServerValidate{后端验证}
    
    ServerValidate -->|用户名已存在| ReturnError1[返回错误: 用户名已存在]
    ServerValidate -->|邮箱已注册| ReturnError2[返回错误: 邮箱已注册]
    ReturnError1 --> ShowServerError[显示服务器错误]
    ReturnError2 --> ShowServerError
    ShowServerError --> Input
    
    ServerValidate -->|验证通过| HashPassword[密码 Bcrypt 加密]
    HashPassword --> CreateUser[创建用户记录]
    CreateUser --> GenerateCode[生成邮箱验证码]
    GenerateCode --> SaveCode[保存验证码到数据库]
    SaveCode --> SendEmail[发送验证邮件]
    SendEmail --> ReturnSuccess[返回注册成功]
    
    ReturnSuccess --> ShowSuccess[显示注册成功提示]
    ShowSuccess --> NavigateLogin[跳转到登录页面]
    NavigateLogin --> End([注册流程结束])
    
    style Start fill:#e1f5e1
    style End fill:#ffe1e1
    style CreateUser fill:#fff4e1
    style SendEmail fill:#e1f0ff
```

---

## 二、用户登录流程

```mermaid
flowchart TD
    Start([用户打开登录页面]) --> Input[输入登录信息]
    Input --> InputCredentials[填写用户名/邮箱和密码]
    
    InputCredentials --> TogglePassword{点击眼睛图标?}
    TogglePassword -->|是| ShowPassword[显示/隐藏密码]
    ShowPassword --> InputCredentials
    TogglePassword -->|否| ClickLogin
    
    ClickLogin[点击登录按钮] --> ValidateForm{前端验证}
    
    ValidateForm -->|字段为空| ShowError[显示: 请输入用户名和密码]
    ShowError --> Input
    
    ValidateForm -->|验证通过| ShowLoading[显示加载动画]
    ShowLoading --> SendRequest[发送登录请求]
    
    SendRequest --> ServerAuth{后端认证}
    
    ServerAuth -->|用户不存在| ReturnError1[返回错误: 用户名或密码错误]
    ServerAuth -->|密码错误| ReturnError2[返回错误: 用户名或密码错误]
    ServerAuth -->|账户被禁用| ReturnError3[返回错误: 账户已被禁用]
    
    ReturnError1 --> LogFailure1[记录登录失败日志]
    ReturnError2 --> LogFailure2[记录登录失败日志]
    ReturnError3 --> LogFailure3[记录登录失败日志]
    
    LogFailure1 --> HideLoading1[隐藏加载动画]
    LogFailure2 --> HideLoading1
    LogFailure3 --> HideLoading1
    HideLoading1 --> ShowAuthError[显示登录失败提示]
    ShowAuthError --> Input
    
    ServerAuth -->|认证成功| VerifyPassword[验证密码]
    VerifyPassword --> UpdateLoginTime[更新最后登录时间]
    UpdateLoginTime --> GenerateTokens[生成 JWT Tokens]
    
    GenerateTokens --> CreateAccessToken[创建 Access Token<br/>有效期: 30分钟]
    GenerateTokens --> CreateRefreshToken[创建 Refresh Token<br/>有效期: 7天]
    
    CreateAccessToken --> LogSuccess[记录登录成功日志]
    CreateRefreshToken --> LogSuccess
    LogSuccess --> ReturnTokens[返回 Tokens]
    
    ReturnTokens --> SaveTokens[保存 Tokens 到本地存储]
    SaveTokens --> HideLoading2[隐藏加载动画]
    HideLoading2 --> NavigateHome[跳转到首页]
    NavigateHome --> End([登录流程结束])
    
    style Start fill:#e1f5e1
    style End fill:#ffe1e1
    style GenerateTokens fill:#fff4e1
    style SaveTokens fill:#e1f0ff
```

---

## 三、Token 验证和刷新流程

```mermaid
flowchart TD
    Start([用户访问受保护页面]) --> GetToken[从本地存储获取 Access Token]
    
    GetToken --> CheckToken{Token 存在?}
    CheckToken -->|不存在| NoToken[未登录状态]
    NoToken --> RedirectLogin[跳转到登录页面]
    RedirectLogin --> End1([需要登录])
    
    CheckToken -->|存在| SendRequest[发送 API 请求<br/>携带 Token]
    
    SendRequest --> ServerValidate{服务器验证 Token}
    
    ServerValidate -->|Token 无效| Return401_1[返回 401 未授权]
    ServerValidate -->|Token 格式错误| Return401_2[返回 401 未授权]
    
    Return401_1 --> ClearToken1[清除本地 Token]
    Return401_2 --> ClearToken1
    ClearToken1 --> RedirectLogin
    
    ServerValidate -->|Token 已过期| CheckExpired{检查过期时间}
    
    CheckExpired --> GetRefreshToken[获取 Refresh Token]
    GetRefreshToken --> RefreshTokenExists{Refresh Token 存在?}
    
    RefreshTokenExists -->|不存在| ClearToken2[清除本地 Token]
    ClearToken2 --> RedirectLogin
    
    RefreshTokenExists -->|存在| SendRefreshRequest[发送刷新请求]
    
    SendRefreshRequest --> ValidateRefreshToken{验证 Refresh Token}
    
    ValidateRefreshToken -->|无效或过期| ClearAllTokens[清除所有 Token]
    ClearAllTokens --> RedirectLogin
    
    ValidateRefreshToken -->|有效| GenerateNewToken[生成新的 Access Token]
    GenerateNewToken --> SaveNewToken[保存新 Token]
    SaveNewToken --> RetryRequest[重试原请求]
    RetryRequest --> ServerValidate
    
    ServerValidate -->|Token 有效| DecodeToken[解码 Token]
    DecodeToken --> GetUserId[获取用户 ID]
    GetUserId --> QueryUser[查询用户信息]
    
    QueryUser --> UserExists{用户存在且激活?}
    UserExists -->|否| Return401_3[返回 401 未授权]
    Return401_3 --> ClearToken1
    
    UserExists -->|是| AttachUser[将用户信息附加到请求]
    AttachUser --> ProcessRequest[处理业务逻辑]
    ProcessRequest --> ReturnResponse[返回响应数据]
    ReturnResponse --> End2([请求成功])
    
    style Start fill:#e1f5e1
    style End1 fill:#ffe1e1
    style End2 fill:#e1ffe1
    style GenerateNewToken fill:#fff4e1
    style ProcessRequest fill:#e1f0ff
```

---

## 四、忘记密码流程

```mermaid
flowchart TD
    Start([用户点击忘记密码]) --> InputEmail[打开忘记密码页面]
    
    InputEmail --> EnterEmail[输入注册邮箱]
    EnterEmail --> ClickSend[点击发送验证码]
    
    ClickSend --> ValidateEmail{验证邮箱格式}
    ValidateEmail -->|格式错误| ShowError1[显示: 请输入有效的邮箱]
    ShowError1 --> InputEmail
    
    ValidateEmail -->|格式正确| SendCodeRequest[发送验证码请求]
    
    SendCodeRequest --> CheckEmail{后端检查邮箱}
    CheckEmail -->|用户不存在| ReturnSuccess1[返回: 验证码已发送<br/>安全措施: 不暴露用户是否存在]
    
    CheckEmail -->|用户存在| GenerateCode[生成6位随机验证码]
    GenerateCode --> SaveCode[保存验证码到数据库<br/>有效期: 10分钟]
    SaveCode --> SendEmail[发送验证码邮件]
    SendEmail --> ReturnSuccess2[返回: 验证码已发送]
    
    ReturnSuccess1 --> ShowSuccess[显示成功提示]
    ReturnSuccess2 --> ShowSuccess
    ShowSuccess --> NavigateVerify[跳转到验证码页面]
    
    NavigateVerify --> InputCode[输入验证码]
    InputCode --> EnterCode[填写6位验证码]
    EnterCode --> ClickVerify[点击验证]
    
    ClickVerify --> ValidateCodeFormat{验证码格式正确?}
    ValidateCodeFormat -->|否| ShowError2[显示: 请输入6位数字]
    ShowError2 --> InputCode
    
    ValidateCodeFormat -->|是| SendVerifyRequest[发送验证请求]
    
    SendVerifyRequest --> CheckCode{后端验证验证码}
    
    CheckCode -->|验证码不存在| ReturnError1[返回: 验证码无效]
    CheckCode -->|验证码已使用| ReturnError2[返回: 验证码已使用]
    CheckCode -->|验证码已过期| ReturnError3[返回: 验证码已过期]
    
    ReturnError1 --> ShowCodeError[显示验证失败]
    ReturnError2 --> ShowCodeError
    ReturnError3 --> ShowCodeError
    ShowCodeError --> InputCode
    
    CheckCode -->|验证码正确| MarkUsed[标记验证码为已使用]
    MarkUsed --> NavigateReset[跳转到重置密码页面]
    
    NavigateReset --> InputNewPassword[输入新密码]
    InputNewPassword --> EnterPassword[填写新密码和确认密码]
    
    EnterPassword --> CheckStrength{检查密码强度}
    CheckStrength -->|弱| ShowWeak[显示密码强度: 弱]
    CheckStrength -->|中| ShowMedium[显示密码强度: 中]
    CheckStrength -->|强| ShowStrong[显示密码强度: 强]
    
    ShowWeak --> EnterPassword
    ShowMedium --> ClickReset
    ShowStrong --> ClickReset
    
    ClickReset[点击重置密码] --> ValidatePassword{验证密码}
    
    ValidatePassword -->|长度不足| ShowError3[显示: 密码至少8位]
    ValidatePassword -->|格式不符| ShowError4[显示: 必须包含字母和数字]
    ValidatePassword -->|两次不一致| ShowError5[显示: 两次密码不一致]
    
    ShowError3 --> InputNewPassword
    ShowError4 --> InputNewPassword
    ShowError5 --> InputNewPassword
    
    ValidatePassword -->|验证通过| SendResetRequest[发送重置密码请求]
    
    SendResetRequest --> HashNewPassword[Bcrypt 加密新密码]
    HashNewPassword --> UpdatePassword[更新数据库密码]
    UpdatePassword --> InvalidateTokens[使所有旧 Token 失效]
    InvalidateTokens --> ReturnResetSuccess[返回重置成功]
    
    ReturnResetSuccess --> ShowResetSuccess[显示重置成功提示]
    ShowResetSuccess --> NavigateLogin[跳转到登录页面]
    NavigateLogin --> End([密码重置完成])
    
    style Start fill:#e1f5e1
    style End fill:#ffe1e1
    style UpdatePassword fill:#fff4e1
    style SendEmail fill:#e1f0ff
```

---

## 五、邮箱验证流程

```mermaid
flowchart TD
    Start([用户注册成功]) --> AutoSend[系统自动发送验证邮件]
    
    AutoSend --> GenerateCode[生成6位验证码]
    GenerateCode --> SaveCode[保存到数据库<br/>类型: email_verify<br/>有效期: 10分钟]
    SaveCode --> SendEmail[发送验证邮件]
    
    SendEmail --> UserReceive[用户收到邮件]
    UserReceive --> CheckExpired1{10分钟内?}
    
    CheckExpired1 -->|超时| CodeExpired[验证码过期]
    CodeExpired --> ClickResend1[点击重新发送]
    ClickResend1 --> GenerateCode
    
    CheckExpired1 -->|未超时| UserAction{用户操作}
    
    UserAction -->|点击邮件链接| OpenApp1[打开 App 验证页面]
    UserAction -->|手动输入| OpenApp2[打开 App 输入验证码]
    
    OpenApp1 --> AutoFillCode[自动填充验证码]
    OpenApp2 --> ManualInput[手动输入验证码]
    
    AutoFillCode --> ClickVerify
    ManualInput --> ClickVerify
    
    ClickVerify[点击验证按钮] --> SendVerifyRequest[发送验证请求]
    
    SendVerifyRequest --> ServerVerify{后端验证}
    
    ServerVerify -->|验证码不匹配| ReturnError1[返回错误: 验证码无效]
    ServerVerify -->|验证码已过期| ReturnError2[返回错误: 验证码已过期]
    ServerVerify -->|验证码已使用| ReturnError3[返回错误: 验证码已使用]
    
    ReturnError1 --> ShowError[显示验证失败]
    ReturnError2 --> ShowError
    ReturnError3 --> ShowError
    
    ShowError --> OfferResend{提供重新发送选项}
    OfferResend --> ClickResend2[点击重新发送]
    ClickResend2 --> CheckLimit{检查发送频率}
    
    CheckLimit -->|超过限制| ShowLimitError[显示: 请稍后再试]
    ShowLimitError --> End1([验证失败])
    
    CheckLimit -->|未超过限制| GenerateCode
    
    ServerVerify -->|验证成功| MarkUsed[标记验证码为已使用]
    MarkUsed --> UpdateUser[更新用户状态<br/>is_verified = true]
    UpdateUser --> ReturnSuccess[返回验证成功]
    
    ReturnSuccess --> ShowSuccess[显示验证成功提示]
    ShowSuccess --> UnlockFeatures[解锁完整功能]
    UnlockFeatures --> End2([邮箱验证完成])
    
    style Start fill:#e1f5e1
    style End1 fill:#ffe1e1
    style End2 fill:#e1ffe1
    style SendEmail fill:#e1f0ff
    style UpdateUser fill:#fff4e1
```

---

## 六、修改密码流程（已登录）

```mermaid
flowchart TD
    Start([用户进入设置页面]) --> ClickChangePassword[点击修改密码]
    
    ClickChangePassword --> OpenChangePage[打开修改密码页面]
    OpenChangePage --> InputPasswords[输入密码信息]
    
    InputPasswords --> EnterOldPassword[输入当前密码]
    EnterOldPassword --> EnterNewPassword[输入新密码]
    EnterNewPassword --> EnterConfirmPassword[确认新密码]
    
    EnterConfirmPassword --> CheckStrength{检查新密码强度}
    CheckStrength -->|弱| ShowWeak[显示: 密码强度弱]
    CheckStrength -->|中| ShowMedium[显示: 密码强度中]
    CheckStrength -->|强| ShowStrong[显示: 密码强度强]
    
    ShowWeak --> EnterNewPassword
    ShowMedium --> ClickSubmit
    ShowStrong --> ClickSubmit
    
    ClickSubmit[点击提交] --> ValidateForm{前端验证}
    
    ValidateForm -->|旧密码为空| ShowError1[显示: 请输入当前密码]
    ValidateForm -->|新密码为空| ShowError2[显示: 请输入新密码]
    ValidateForm -->|长度不足| ShowError3[显示: 密码至少8位]
    ValidateForm -->|格式不符| ShowError4[显示: 必须包含字母和数字]
    ValidateForm -->|两次不一致| ShowError5[显示: 两次密码不一致]
    ValidateForm -->|新旧相同| ShowError6[显示: 新密码不能与旧密码相同]
    
    ShowError1 --> InputPasswords
    ShowError2 --> InputPasswords
    ShowError3 --> InputPasswords
    ShowError4 --> InputPasswords
    ShowError5 --> InputPasswords
    ShowError6 --> InputPasswords
    
    ValidateForm -->|验证通过| GetToken[获取当前用户 Token]
    GetToken --> SendChangeRequest[发送修改密码请求]
    
    SendChangeRequest --> VerifyToken{验证 Token}
    VerifyToken -->|无效| Return401[返回 401 未授权]
    Return401 --> RedirectLogin[跳转到登录页]
    RedirectLogin --> End1([需要重新登录])
    
    VerifyToken -->|有效| GetUser[获取当前用户信息]
    GetUser --> VerifyOldPassword{验证当前密码}
    
    VerifyOldPassword -->|密码错误| ReturnError[返回: 当前密码错误]
    ReturnError --> ShowPasswordError[显示密码错误提示]
    ShowPasswordError --> InputPasswords
    
    VerifyOldPassword -->|密码正确| CheckNewPassword{检查新密码}
    CheckNewPassword -->|与旧密码相同| ReturnSameError[返回: 新旧密码不能相同]
    ReturnSameError --> ShowPasswordError
    
    CheckNewPassword -->|密码不同| HashNewPassword[Bcrypt 加密新密码]
    HashNewPassword --> UpdateDatabase[更新数据库密码]
    UpdateDatabase --> InvalidateOldTokens[使所有旧 Token 失效]
    InvalidateOldTokens --> GenerateNewTokens[生成新的 Tokens]
    GenerateNewTokens --> LogChange[记录密码修改日志]
    LogChange --> SendNotification[发送邮件通知用户]
    SendNotification --> ReturnSuccess[返回修改成功]
    
    ReturnSuccess --> SaveNewTokens[保存新 Tokens]
    SaveNewTokens --> ShowSuccess[显示修改成功提示]
    ShowSuccess --> End2([密码修改完成])
    
    style Start fill:#e1f5e1
    style End1 fill:#ffe1e1
    style End2 fill:#e1ffe1
    style UpdateDatabase fill:#fff4e1
    style SendNotification fill:#e1f0ff
```

---

## 七、登出流程

```mermaid
flowchart TD
    Start([用户点击登出按钮]) --> ShowConfirm{显示确认对话框}
    
    ShowConfirm -->|取消| End1([取消登出])
    
    ShowConfirm -->|确认| GetToken[获取当前 Token]
    GetToken --> SendLogoutRequest[发送登出请求到服务器]
    
    SendLogoutRequest --> ServerProcess{服务器处理}
    
    ServerProcess --> RevokeToken[撤销 Refresh Token<br/>标记为已撤销]
    RevokeToken --> LogLogout[记录登出日志]
    LogLogout --> ReturnSuccess[返回登出成功]
    
    ReturnSuccess --> ClearLocalTokens[清除本地存储的 Tokens]
    ClearLocalTokens --> ClearAccessToken[删除 Access Token]
    ClearLocalTokens --> ClearRefreshToken[删除 Refresh Token]
    
    ClearAccessToken --> ClearUserData[清除本地用户数据]
    ClearRefreshToken --> ClearUserData
    
    ClearUserData --> ResetState[重置应用状态]
    ResetState --> NavigateLogin[跳转到登录页面]
    NavigateLogin --> ShowMessage[显示登出成功提示]
    ShowMessage --> End2([登出完成])
    
    style Start fill:#e1f5e1
    style End1 fill:#ffe1e1
    style End2 fill:#e1ffe1
    style RevokeToken fill:#fff4e1
    style ClearLocalTokens fill:#e1f0ff
```

---

## 八、自动登录流程

```mermaid
flowchart TD
    Start([App 启动]) --> ShowSplash[显示启动页<br/>3秒]
    
    ShowSplash --> CheckToken[检查本地 Token]
    
    CheckToken --> TokenExists{Token 存在?}
    
    TokenExists -->|不存在| NoToken[未登录状态]
    NoToken --> NavigateAuth[跳转到登录/注册页]
    NavigateAuth --> End1([需要登录])
    
    TokenExists -->|存在| CheckExpired{Token 是否过期?}
    
    CheckExpired -->|Access Token 有效| ValidateToken[验证 Token]
    
    ValidateToken --> SendMeRequest[请求当前用户信息<br/>GET /api/auth/me]
    
    SendMeRequest --> ServerValidate{服务器验证}
    
    ServerValidate -->|Token 无效| ClearInvalidToken[清除无效 Token]
    ServerValidate -->|用户不存在| ClearInvalidToken
    ServerValidate -->|账户被禁用| ClearInvalidToken
    
    ClearInvalidToken --> NavigateAuth
    
    ServerValidate -->|验证成功| GetUserInfo[获取用户完整信息]
    GetUserInfo --> SaveUserState[保存用户状态到内存]
    SaveUserState --> CheckVerified{邮箱已验证?}
    
    CheckVerified -->|未验证| ShowVerifyBanner[显示验证提示]
    ShowVerifyBanner --> NavigateHome1[跳转到首页]
    
    CheckVerified -->|已验证| NavigateHome2[跳转到首页]
    
    NavigateHome1 --> End2([自动登录成功])
    NavigateHome2 --> End2
    
    CheckExpired -->|Access Token 过期| GetRefreshToken[获取 Refresh Token]
    
    GetRefreshToken --> RefreshExists{Refresh Token 存在?}
    
    RefreshExists -->|不存在| ClearAllTokens[清除所有 Token]
    ClearAllTokens --> NavigateAuth
    
    RefreshExists -->|存在| SendRefreshRequest[发送刷新请求<br/>POST /api/auth/refresh]
    
    SendRefreshRequest --> ValidateRefresh{验证 Refresh Token}
    
    ValidateRefresh -->|无效或过期| ClearExpiredTokens[清除过期 Token]
    ClearExpiredTokens --> NavigateAuth
    
    ValidateRefresh -->|有效| GenerateNew[生成新 Access Token]
    GenerateNew --> SaveNewToken[保存新 Token]
    SaveNewToken --> ValidateToken
    
    style Start fill:#e1f5e1
    style End1 fill:#ffe1e1
    style End2 fill:#e1ffe1
    style GenerateNew fill:#fff4e1
    style SaveUserState fill:#e1f0ff
```

---

## 九、完整认证架构流程

```mermaid
flowchart LR
    subgraph Client["前端客户端 (React Native)"]
        UI[用户界面]
        Storage[本地存储<br/>AsyncStorage]
        API[API 客户端<br/>Axios]
    end
    
    subgraph Server["后端服务器 (FastAPI)"]
        Router[API 路由<br/>/api/auth/*]
        Service[认证服务<br/>AuthService]
        Security[安全模块<br/>JWT/Bcrypt]
    end
    
    subgraph Database["数据库 (PostgreSQL)"]
        Users[(用户表<br/>users)]
        Codes[(验证码表<br/>verification_codes)]
        Tokens[(刷新令牌表<br/>refresh_tokens)]
        Logs[(登录日志<br/>login_logs)]
    end
    
    subgraph External["外部服务"]
        Email[邮件服务<br/>SMTP]
        SMS[短信服务<br/>阿里云]
    end
    
    UI -->|1. 用户操作| API
    API -->|2. HTTP 请求| Router
    Router -->|3. 调用| Service
    Service -->|4. 加密/验证| Security
    Service -->|5. 数据操作| Users
    Service -->|6. 保存验证码| Codes
    Service -->|7. Token 管理| Tokens
    Service -->|8. 记录日志| Logs
    Service -->|9. 发送通知| Email
    Service -->|10. 发送通知| SMS
    Router -->|11. 返回响应| API
    API -->|12. 保存 Token| Storage
    API -->|13. 更新界面| UI
    
    style Client fill:#e1f5e1
    style Server fill:#fff4e1
    style Database fill:#e1f0ff
    style External fill:#ffe1e1
```

---

## 十、数据流向图

```mermaid
flowchart TD
    subgraph Input["用户输入"]
        I1[用户名/邮箱]
        I2[密码]
        I3[验证码]
    end
    
    subgraph Frontend["前端处理"]
        F1[输入验证]
        F2[格式检查]
        F3[密码强度检测]
        F4[API 请求封装]
    end
    
    subgraph Backend["后端处理"]
        B1[接收请求]
        B2[数据验证]
        B3[业务逻辑]
        B4[密码加密/验证]
        B5[Token 生成]
        B6[数据库操作]
    end
    
    subgraph Database["数据存储"]
        D1[(用户表)]
        D2[(验证码表)]
        D3[(Token 表)]
        D4[(日志表)]
    end
    
    subgraph Response["响应处理"]
        R1[Token 返回]
        R2[用户信息返回]
        R3[错误信息返回]
    end
    
    subgraph Storage["本地存储"]
        S1[保存 Access Token]
        S2[保存 Refresh Token]
        S3[保存用户信息]
    end
    
    I1 --> F1
    I2 --> F1
    I3 --> F1
    
    F1 --> F2
    F2 --> F3
    F3 --> F4
    
    F4 -->|HTTP POST| B1
    B1 --> B2
    B2 --> B3
    B3 --> B4
    B4 --> B5
    B5 --> B6
    
    B6 --> D1
    B6 --> D2
    B6 --> D3
    B6 --> D4
    
    B6 --> R1
    B6 --> R2
    B2 --> R3
    
    R1 --> S1
    R1 --> S2
    R2 --> S3
    
    S1 -.->|后续请求携带| B1
    S2 -.->|Token 刷新| B1
    
    style Input fill:#e1f5e1
    style Frontend fill:#fff4e1
    style Backend fill:#e1f0ff
    style Database fill:#ffe1e1
    style Response fill:#f0e1ff
    style Storage fill:#e1ffe1
```

---

## 十一、Token 生命周期图

```mermaid
sequenceDiagram
    participant U as 用户
    participant C as 客户端
    participant S as 服务器
    participant D as 数据库
    
    Note over U,D: 登录阶段
    U->>C: 输入用户名密码
    C->>S: POST /api/auth/login
    S->>D: 验证用户凭据
    D-->>S: 用户信息
    S->>S: 生成 Access Token (30分钟)
    S->>S: 生成 Refresh Token (7天)
    S->>D: 保存 Refresh Token
    S-->>C: 返回 Tokens
    C->>C: 保存到本地存储
    C-->>U: 登录成功，跳转首页
    
    Note over U,D: 正常请求阶段
    U->>C: 访问需要认证的页面
    C->>S: GET /api/posts<br/>Header: Authorization: Bearer {access_token}
    S->>S: 验证 Access Token
    S->>D: 获取数据
    D-->>S: 返回数据
    S-->>C: 返回响应
    C-->>U: 显示内容
    
    Note over U,D: Token 过期阶段
    U->>C: 30分钟后再次访问
    C->>S: GET /api/posts<br/>Header: Authorization: Bearer {expired_token}
    S->>S: 验证 Token
    S-->>C: 401 Token 已过期
    C->>C: 拦截器捕获 401
    C->>S: POST /api/auth/refresh<br/>Body: {refresh_token}
    S->>D: 验证 Refresh Token
    D-->>S: Token 有效
    S->>S: 生成新 Access Token
    S-->>C: 返回新 Token
    C->>C: 更新本地存储
    C->>S: 重试原请求<br/>Header: Authorization: Bearer {new_token}
    S-->>C: 返回数据
    C-->>U: 显示内容（用户无感知）
    
    Note over U,D: Refresh Token 过期
    U->>C: 7天后访问
    C->>S: POST /api/auth/refresh<br/>Body: {expired_refresh_token}
    S->>D: 验证 Refresh Token
    D-->>S: Token 已过期
    S-->>C: 401 需要重新登录
    C->>C: 清除所有 Token
    C-->>U: 跳转到登录页
    
    Note over U,D: 主动登出
    U->>C: 点击登出按钮
    C->>S: POST /api/auth/logout
    S->>D: 撤销 Refresh Token
    D-->>S: 已撤销
    S-->>C: 登出成功
    C->>C: 清除本地 Token
    C-->>U: 跳转到登录页
```

---

## 十二、安全防护流程

```mermaid
flowchart TD
    Start([用户请求]) --> Layer1{安全层 1<br/>HTTPS 加密}
    
    Layer1 -->|未加密| Reject1[拒绝请求<br/>强制 HTTPS]
    Reject1 --> End1([请求被拒绝])
    
    Layer1 -->|加密传输| Layer2{安全层 2<br/>CORS 验证}
    
    Layer2 -->|非法来源| Reject2[拒绝跨域请求]
    Reject2 --> End1
    
    Layer2 -->|合法来源| Layer3{安全层 3<br/>速率限制}
    
    Layer3 -->|超过限制| Reject3[返回 429<br/>请求过于频繁]
    Reject3 --> End1
    
    Layer3 -->|正常频率| Layer4{安全层 4<br/>输入验证}
    
    Layer4 -->|恶意输入| Reject4[拒绝请求<br/>输入不合法]
    Reject4 --> End1
    
    Layer4 -->|输入合法| Layer5{安全层 5<br/>Token 验证}
    
    Layer5 -->|无效 Token| Reject5[返回 401<br/>未授权]
    Reject5 --> End1
    
    Layer5 -->|有效 Token| Layer6{安全层 6<br/>权限检查}
    
    Layer6 -->|权限不足| Reject6[返回 403<br/>禁止访问]
    Reject6 --> End1
    
    Layer6 -->|权限充足| Layer7{安全层 7<br/>SQL 注入防护}
    
    Layer7 -->|检测到攻击| Reject7[拒绝请求<br/>记录日志]
    Reject7 --> End1
    
    Layer7 -->|安全| Layer8{安全层 8<br/>XSS 防护}
    
    Layer8 -->|检测到脚本| Reject8[过滤恶意脚本]
    Reject8 --> End1
    
    Layer8 -->|安全| ProcessRequest[处理业务逻辑]
    
    ProcessRequest --> LogAccess[记录访问日志]
    LogAccess --> ReturnResponse[返回响应]
    ReturnResponse --> End2([请求成功])
    
    style Start fill:#e1f5e1
    style End1 fill:#ffe1e1
    style End2 fill:#e1ffe1
    style ProcessRequest fill:#fff4e1
```

---

## 使用说明

### 如何查看流程图

这些流程图使用 **Mermaid** 语法编写，支持在以下环境中查看：

1. **GitHub/GitLab**
   - 直接在仓库中查看 Markdown 文件，流程图会自动渲染

2. **VS Code**
   - 安装 "Markdown Preview Mermaid Support" 插件
   - 预览 Markdown 文件即可看到流程图

3. **在线工具**
   - [Mermaid Live Editor](https://mermaid.live/)
   - 复制流程图代码到编辑器中查看

4. **Notion / Confluence**
   - 支持 Mermaid 图表的知识库平台

### 流程图说明

- 🟢 **绿色节点**：流程起点
- 🔴 **红色节点**：流程终点（失败/错误）
- 🟡 **黄色节点**：关键操作（数据库写入、Token 生成等）
- 🔵 **蓝色节点**：外部服务调用（邮件、短信）
- 💎 **菱形节点**：判断/分支
- 📦 **矩形节点**：处理步骤

### 对应的设计文档

详细的实现代码和 API 文档请参考：
- **用户登录系统设计文档.md** - 完整的技术实现
- **Tennis-Frog设计索引.md** - Figma 设计对照
- **技术实践路径.md** - 开发实践指南

---

**文档版本：** v1.0  
**创建日期：** 2025-11-03  
**流程图总数：** 12 个

希望这些流程图能帮助你更好地理解用户登录系统的各个环节！🎯

